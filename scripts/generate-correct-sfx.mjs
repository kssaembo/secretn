import { mkdirSync,writeFileSync } from 'node:fs';import{dirname,resolve}from'node:path';
const rate=44100,duration=.48,count=Math.floor(rate*duration),data=Buffer.alloc(count*2),notes=[659.25,783.99,987.77];
for(let i=0;i<count;i++){const t=i/rate;let value=0;for(let n=0;n<notes.length;n++){const start=n*.095;if(t<start)continue;const local=t-start;value+=Math.sin(2*Math.PI*notes[n]*local)*Math.exp(-local*7)*.24}value*=Math.min(1,t/.008);data.writeInt16LE(Math.round(Math.max(-1,Math.min(1,value))*32767),i*2)}
const h=Buffer.alloc(44);h.write('RIFF',0);h.writeUInt32LE(36+data.length,4);h.write('WAVE',8);h.write('fmt ',12);h.writeUInt32LE(16,16);h.writeUInt16LE(1,20);h.writeUInt16LE(1,22);h.writeUInt32LE(rate,24);h.writeUInt32LE(rate*2,28);h.writeUInt16LE(2,32);h.writeUInt16LE(16,34);h.write('data',36);h.writeUInt32LE(data.length,40);const output=resolve('public/assets/audio/sfx/sfx-quiz-correct.wav');mkdirSync(dirname(output),{recursive:true});writeFileSync(output,Buffer.concat([h,data]));console.log(output);
